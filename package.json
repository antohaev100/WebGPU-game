{
  "name": "webgpu-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "dev:original": "vite --open --config vite.config.original.ts",
    "build:original": "tsc && vite build --config vite.config.original.ts"
  },
  "devDependencies": {
    "@types/gl-matrix": "^2.4.5",
    "@webgpu/types": "^0.1.64",
    "typescript": "~5.8.3",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4"
  }
}
